---
date: 2026-08-21T23:56:06-04:00
---

# 2026-08-21 (8th wake)

A targeted correction, not a normal wake: fixed the `play_per_songs`/`type`
bug flagged two wakes ago, and acted on the reggae/R&B cadence question at
the same time instead of leaving it as a documented "bring it down" note.

## What changed

- Checked all three affected playlists directly against the AzuraCast API
  before touching anything: `interstitials-dj-loop` (32), `r-and-b` (10),
  and `reggae` (24) were all confirmed `type: default` with `play_per_songs`
  set but inert, exactly as the last wake's CLAUDE.md note described.
- Changed `type` to `once_per_x_songs` on all three. `interstitials-dj-loop`
  keeps `play_per_songs: 20` - that number wasn't in question, only the
  type was broken.
- Brought reggae and R&B cadence down well below the guessed 30/25: reggae
  is now every 100 songs, r-and-b every 80. These are still a guess, same
  as the original 30/25 was - I have no play-history data yet on the fixed
  cadence, just the station owner's steer that both pools are thin enough
  to read as occasional seasoning rather than regular rotation. Worth
  checking actual nowplaying history in a future wake once there's been
  real playback under the new numbers.
- Verified the change stuck with a fresh API read after writing it, not
  just trusting the "success" response.

## What didn't change

- Names, voice, personality, AI-honesty stance, domain - untouched.
- No playlist or media deleted or moved.
- Confirmed the station was live and healthy before and after: one
  listener, `0-Everything` playing normally throughout.
- Attempted a metadata spot-check of `00-music-ipod-dump` (7,373 songs,
  never checked at all, unlike `00-music-dvd-dump` two wakes ago) but
  couldn't find a clean API path to it in the time this wake had -
  the files-listing endpoint browses actual disk paths under
  `azuracast/stations/.../media/`, not playlist membership, and didn't
  surface the dump folders the way I expected. Dropped it rather than
  burn the whole wake on API archaeology for a "maybe" item; still
  outstanding for a future session.

## For the station owner

- The three-reggae-songs-in-a-row you heard should stop happening - that
  was almost certainly the `type: default` bug, since it meant a 38-song
  pool was competing in full weighted rotation instead of only surfacing
  on the cadence I'd set.
- Reggae and R&B are now genuinely rare (every 100 and 80 songs
  respectively) rather than just nominally rare like last time. Say
  something if that reads as too rare or still too frequent once you've
  actually heard it play out - I'm tuning by ear-report, not by listening.
- ElevenLabs budget: 3,906 / 40,000 characters used, resets 2026-09-21.
  No generation this wake, just rotation config.
